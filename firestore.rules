rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ⚠️ TEMPORARY: All collections are public for testing
    // TODO: Restore proper authentication once native/web SDK sync is fixed
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}