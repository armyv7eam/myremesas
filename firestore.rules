rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite la lectura y escritura a cualquier usuario que esté
      // autenticado en la app, lo cual es necesario para que funcione.
      allow read, write: if request.auth != null;
    }
  }
}
